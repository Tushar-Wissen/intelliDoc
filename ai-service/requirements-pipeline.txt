# Heavy parsing/OCR runtimes. Lazy-imported by the pipeline; required in the
# ai-service image, not for unit tests (those use PyMuPDF/python-docx fallbacks).
docling>=2.0.0,<3.0.0
paddleocr>=2.8.0,<3.0.0
paddlepaddle>=2.6.0,<3.1.0
# BGE-M3 dense embeddings (Story 4.1). Lazy-imported; unit tests inject a fake encoder.
sentence-transformers>=3.0.0,<4.0.0
